import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';  // Caminho relativo corrigido
import './App.css';      // Caminho relativo corrigido

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);